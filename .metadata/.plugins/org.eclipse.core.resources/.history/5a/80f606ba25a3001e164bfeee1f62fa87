package packagee16_Heaps;

public class P1_Addition {
	
	/*
	 * Step 1 : first add at the last index of the array
	 * 
	 * Step 2 : format the heap while loop code
	 * 
	 * Note : 
	 * if currNode = i
	 * then 
	 *   currNode.left  = 2i+1
	 *   currNode.right = 2i+2
	 *   
	 *if currNode.subNode (this can be left or right) = i
	 *then 
	 *   root of that is = (i-1)/2
	 *   
	 * 
	 * solution:
	 * so as long as the newly added data is smaller than the root, swap code
	 * 
	 */

	public static void main(String[] args) {
		
		

	}

}
